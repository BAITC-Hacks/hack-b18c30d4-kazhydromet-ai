{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "python -m app.pipeline && exec python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1"
  }
}
