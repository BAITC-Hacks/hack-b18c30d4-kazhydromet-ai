# Проверка перед сдачей — 23.09.2026

## Изменения и коммиты

- `f4723cd`: печатный HTML-отчёт, контекст дня/перечня для AI, 8 инструментов, три сценария входа и восстановление загрузки.
- `5493619`: одинаковый порядок клиентов при равных баллах на Windows и Linux; роли, пороги и формула приоритета сохранены.
- `app/pipeline.py:388`: нестабильная сортировка меняла gid на границе топ-50 между версиями pandas/NumPy. Исправление: priority DESC, gid ASC; то же для лидеров кластеров.
- `app/graph.py:22`: такой же порядок закреплён в API, включая глобальный rank. Контрольные ранги — 5 / 17 / 584.
- Все поля для каждого из 2 248 gid до/после исправления сортировки совпали при сравнении по gid.
- AI больше не обрезает ошибочный номер из 19 цифр; явные gid/даты важнее контекста; «От кого он получает?» сохраняет выбранного клиента.
- Мобильный переход к AI больше не прячет заголовок под навигацией.

## Реальные проверки

- `scripts/verify.py` объединяет проверки; JSON содержит commit, признак изменённой рабочей папки, stdout, exit code, время и SHA-256 восьми файлов.
- Windows, `5493619`: **21/21 CSV + 24/24 HTTP**, exit 0. Пересчёт валидатора 4,97 с; pipeline отдельного gate 4,0 с.
- Linux / WSL2, Python 3.12.3, uv 0.12.18: **21/21 + 24/24**, exit 0. Pipeline 4,1 с; пересчёт валидатора 3,92 с.
- Чистый клон взят из локального Git без `.env`; первоначально проверены pip и отдельно uv venv / uv pip install. После исправления сортировки клон обновлён до `5493619`, зависимости проверены uv, пересчёт и проверки повторены.
- Три raw Parquet, три CSV и `graph.json` совпали с Windows **побайтно**. У `summary.json` отличается только seconds: 4,0 / 4,1.
- Доказательства: [Windows](VERIFICATION-WINDOWS.json), [Linux](VERIFICATION-LINUX.json), [сравнение SHA-256](REPRODUCIBILITY.json).
- Полный реальный вывод CSV-валидатора: [VALIDATION.txt](VALIDATION.txt), 21 строка OK, включая порядок равных приоритетов.
- AI: 44 проверки mock/SDK без LLM; 32 браузерных проверки контекста и восстановления; 7 POST перехвачены только в тесте.
- Живой OpenAI: HTTP 200, mode=live, 5,53 с, вызов daily_transfers. За 23.07.2026 у контрольного координатора: 6 входящих / 385 900 ₸, 2 исходящих / 479 000 ₸.
- Браузер: 20 проверок сценариев; 19 отчёта; 14 полировки охвата. Размеры 1280 и 390 без переполнения; основной экран и 3 прямые ссылки + запасной проверены на 1600, без внешних ресурсов и ошибок JS.
- Печатная раскладка проверена в print media; PDF-файл не создавался. Сохранение PDF выполняет браузер через печать.
- HTTP smoke сам остановил свои серверы. Порт 8000 владельца не трогали; содержимое `.env` не просматривалось, ключи не выводились и не коммитились.

## Скриншоты

- [Главная](screenshots/main.png), [координатор](screenshots/coordinator.png), [аномалия](screenshots/anomaly.png), [обрыв](screenshots/truncated.png), [отчёт](screenshots/report.png).
- Дополнительные просмотренные снимки: `tmp/review-codex-2/design-qa/report-preview/`, `chat-context-ui/mobile-chat-390-fixed.png`, `hour-ui/backup.png`.

## Сдача и оставшиеся ограничения

- Форма Astana Hub подтвердила: командный репозиторий приватный, создан платформой; организатор имеет доступ для проверки. После дедлайна push блокируется.
- Название, описание и 10 технологий заполнены; финальная кнопка сдачи пока не нажата. Текст: [SUBMISSION.md](SUBMISSION.md).
- Общего URL пока нет. Подготовлены инструкции хостинга и предложен временный Cloudflare Tunnel; решение о публичном доступе ожидается.
- Linux / WSL2 проверен; сборка на Railway не проверена. NVIDIA не использовалась.
- Роли не проверены по размеченной истине; баллы не являются вероятностью вины. Охват операций не равен охвату преступной деятельности.
- Репетиция на проекторе и фактическая отправка решения остаются финальными действиями команды.
