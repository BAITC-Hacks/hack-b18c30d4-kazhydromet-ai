# Дизайн и сценарий аналитика — 23.09.2026

## Реализовано и отправлено
- `6825fb2`: независимые задания Yerbu (QA) и Abylaykhan7 (отчёт).
- `e33b9da`: автоматический снимок первой версии дизайна; `9d916da`: законченная полировка и исправления.
- `60602a9` (Abylaykhan7): полноценный отчёт, аномалии/маршруты/обрыв/раздельные запросы; принято после 20 проверок.
- `6aba32e` (Yerbu): независимый QA; подтверждённые дефекты исправлены в `9d916da`.
- `d456101`: режим «Проследить переводы», API `/api/timeline/{gid}`, календарь, воспроизведение и просмотр конкретного дня.
- Единый локальный дизайн: навигация, три KPI, понятные карточки, схема, доступное действие «В перечень».
- Перечень: до 100 строковых gid, сохранение только в браузере, отмена удаления, актуальные карточки, экспорт выбранных.
- Чат: история и контекст клиента; явные gid в вопросе не дублируются и имеют приоритет.
- Дневной режим: реальные суммы, количества и пары из Parquet; цвета стрелок различают входящие/исходящие.
- Входящие вне выборки и исходящие 4-го колена неизвестны; порядок операций внутри дня и происхождение средств не установлены.

## Исправленные реальные дефекты
- `web/graph.html:66`: vis-network удалял `#netMsg` из контейнера → следующий поиск падал; сообщение вынесено наружу.
- `web/index.html:133`: история клиента заменялась → Back терял предыдущую карточку; добавлена корректная история переходов.
- `web/index.html:136`: чат терял историю/контекст; при добавлении контекста исключены повторы явно названных gid.
- `web/index.html:81`: метки перечня исчезали после перерисовки топа; теперь восстанавливаются из сохранённого выбора.
- `web/app.css`: увеличены контраст и размер пояснений перечня; мобильные ссылки прокручивают к карточке.
- `app/report.py:29`: неизвестный gid мог содержать HTML в Markdown; `_cell` экранирует HTML.
- `web/timeline.js:182`: после неизвестного gid кнопка дневного режима оставалась доступной; состояние обновляется при смене графа.

## Реальные проверки
- Перечень: 22/22 Chrome; диалог, экспорт ровно выбранных gid, reload, очистка/отмена, неизвестный gid.
- Навигация/клавиатура/адаптация: 28/28; дополнительно аномалии и пересчёт 12/12; запасной экран 14/14.
- Чат: 20/20. Один настоящий HTTP-запрос: 200, mode=live, 8,54 с, инструмент node_card.
- Четыре запроса fallback проверены через перехват браузерного запроса и настоящий `_mock`; это не живые LLM-запросы.
- Timeline: 10 проверок всех 2 248 узлов и 4 840 транзакций; точное совпадение дневных пар/сумм, 444 обрыва; 45,72 с.
- Timeline API: 23/23; браузер: 25/25, включая все 31 день, Back, смену клиента и пересчёт (6,939 с).
- Размеры 1600×1000, 1280×800, 390×844: без горизонтального переполнения; проверенные сценарии без ошибок JS и внешних запросов.
- Эмуляция DPR 1,25 проверена; это не проверка Ctrl+Plus 125% в обычном браузере.
- Контрольный запуск `timeline-release`: pipeline 3,9 с; валидатор 21/21 OK, свежий пересчёт 3,94 с; health HTTP 200; 8012 остановлен.
- Роли, THRESHOLDS и формула приоритета в этой части не менялись; новых зависимостей нет.

## Артефакты и остаток
- Снимки: `docs/screenshots/main.png`, `coordinator.png`, `anomaly.png`, `truncated.png`, `routes.png`, `review-list.png`, `timeline.png`.
- Полный реальный вывод валидатора: `tmp/review-codex-2/timeline-release-validator.txt` (21 строка OK, exit 0).
- Логи Chrome: `tmp/review-codex-2/product`, `chat-product`, `design-qa`, `timeline`; локальные тестовые файлы не коммитятся.
- Дополнительный QA и минутный сценарий переданы Yerbu и Abylaykhan7 в двух новых PROMPT-файлах.
- До сдачи: репетиция на проекторе и ручная отправка решения на платформе. Сравнение с кодом конкурентов не проводилось.
- Ограничения прежнего аудита и заблокированное удаление временных клонов: `docs/REVIEW-codex-2.md`.
