# Yerbu — финальная проверка отличительного сценария

Твой предыдущий QA получен; P1 поиска запасного экрана, метки перечня и контраст исправлены в `9d916da`. Не редактируй исходники. Главный Codex добавляет режим «Проследить переводы» / «Потоки по дням», ожидается отдельный следующий коммит.

После его появления: `git pull --rebase`, прочитай STATUS. На своём компьютере проверь `127.0.0.1:8012`, `.env` не читай, порт 8000 не трогай. Единственный файл для записи — `docs/QA-TIMELINE-YERBU.md`.

1. На главной нажми «Проследить переводы»: выбран реальный клиент, меняются даты, суммы и стрелки. Пауза, соседний день, шкала и возврат «Весь период» работают.
2. Дневные суммы и пары со стрелками сверь с `/api/timeline/100000008603629100`; время внутри дня и происхождение одних средств не должны выдаваться за установленные факты.
3. На `100000003037476100` исходящие должны быть «не наблюдаются / обрыв», а не установленный нулевой расход. Изолированный seed должен открываться без падения.
4. Переключи клиента, раскраску, топ-50, затем повтори пересчёт. Проверь мобильный вид, Tab/Space/стрелки на шкале, отсутствие ошибок консоли и внешних запросов. Повтори исправленный поиск `/graph.html`.
5. Оцени первые 10 секунд глазами жюри: видно ли, как начать, что означает дата и куда ведёт следующий шаг аналитика. Запиши только фактически проверенное, до 30 строк; выдели P1/P2 и точные шаги.

Коммитить только `docs/QA-TIMELINE-YERBU.md`, pull --rebase, push. Не меняй STATUS и другие документы. Желательно закончить до 16:25.
