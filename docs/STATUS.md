# Статус команды

Общая память команды и её агентов. Codex и Claude на трёх ноутбуках не видят друг друга напрямую,
но все читают этот файл через git — значит, каждый в курсе, что делают остальные.

**Правило:** взялся за задачу — обнови свою строку и запушь. Занимает 15 секунд, экономит час на разборках,
кто что сломал. Освежить картину у себя: `.\scripts\sync.ps1`

| Кто | Над чем сейчас | Статус | Что нужно от других | Обновлено |
|---|---|---|---|---|
| **Ali** — бэкенд и агент, `app/` | Готово и проверено: pipeline, API (/api/overview, /api/graph, /api/node/{gid}, /api/top?n=&role=, /api/clusters, /api/clusters/{id}, /api/path?src=&dst=, POST /api/recompute), агент с 6 инструментами над графом, мок без ключа отвечает по графу. **gid везде строкой** (18 цифр > JS number). Учтено ревью Codex (docs/REVIEW-codex.md): **/api/path теперь `paths: [{steps:[...], chronology_ok, dates}]` + `note`** (проверка дат: возможен ли маршрут денег по времени); /api/top отдаёт `why` и глобальный `rank`; у узлов graph.json есть `label` (последние 10 цифр gid). Валидатор: `.\.venv\Scripts\python scripts\check_outputs.py` — 21/21 OK | в работе | Yerbu: подключай /api/graph и /api/node/{gid}; кнопки-подсказки для чата: «Кого проверять первым?», «Почему 100000008603629100 координатор?», «Кто собирает деньги с этих пятерых: 100000003684369100, 100000006866783100, 100000003016635100, 100000000343175100, 100000002527114100?» | 23.09.2026 14:05 |
| **Yerbu** — интерфейс и демо, `web/` | Экран «Граф денег»: топ-50, направленный граф с физической раскладкой, окрестность на 2 шага, карточка узла, кластеры, чат, ссылки `#gid`, Markdown-отчёт; библиотеки в `web/vendor/` | **index.html готов к демо**; в браузере проверены топ-150, gid `100000008603629100` и вся сеть (2 248 узлов), ошибок консоли нет; `/api/report?top=20` отдаёт Markdown | Ali: переключить корень `/` на `/index.html`; команде: репетиция на проекторе | 23.09.2026 14:23 |
| **Abylaikhan** — продукт, данные, питч, `docs/` | README, схема и питч сверены с упрощённым главным экраном и новым контрактом `plain`; в README добавлены снимки экрана, в питче — прямые ссылки на первого узла и аномалию | 21/21 проверка выгрузок; `/`, `/graph.html`, `/api/health`, карточка аномалии и сводка отвечают; 37 узлов с флагами сверены | Команде: репетиция на проекторе, включая переключение на запасной экран для устойчивости и оба вопроса к чату | 23.09.2026 15:11 |
| **Ali + Codex** — продукт и финальная проверка | Новый дизайн, выбранный перечень, дневное воспроизведение переводов; интеграция QA Yerbu и отчёта Abylaykhan7 | **Готово: 21/21 OK**, дневной UI 25/25, API 23/23; `9d916da`, `d456101`; отчёты `docs/DESIGN-codex.md` и `docs/REVIEW-codex-2.md` | Репетиция и финальный QA по новым PROMPT-файлам; ручная сдача на платформе | 23.09.2026 16:19 |

Codex: отчёт /api/report готов

Codex, 23.09.2026 16:47: по запросу Ali добавляю «Что даст следующая проверка»: `GET /api/review-coverage?gids=a,b` — объединение видимых транзакций выбранных клиентов без двойного счёта и кандидат из топ-50, добавляющий максимум новых операций. Пустой перечень начинает с первого по приоритету; роли/приоритет не меняются. Файлы: `app/coverage.py`, точечный API в main.py, `web/review-list.js`. NVIDIA не используем; действующий AI — OpenAI.

Контракт расширен: `/api/health` сохраняет `ok/model/live`, добавляет `provider/config_error/public_demo`. При `PUBLIC_DEMO=1` изменяющие выгрузку POST возвращают 403; чат ограничен 2 одновременными запросами и 20 запросами в минуту на процесс, история — 24 000 символов. Локальный режим прежний. Роли и расчёт не меняются.

Codex, 23.09.2026 16:38: закрыты три дефекта из `docs/QA-TIMELINE-YERBU.md`; Chrome 7/7, валидатор 21/21. README начинается с назначения, запуска, технологий и проверки. Продолжаю контрастный интерфейс и направленный просмотр потоков; проверяю свежую поддержку NVIDIA. Общедоступный хостинг пока не настроен.

Codex, 23.09.2026 15:47: новая продуктовая полировка по запросу Ali. Главный Codex владеет `web/index.html`, `web/app.css`, `web/review-list.js` и визуальной проверкой. Yerbu — только `docs/QA-PC2.md` (промпт `docs/PROMPT-PC2-QA.md`); Abylaykhan7 — только `app/report.py` и `docs/REPORT-PC3.md` (промпт `docs/PROMPT-PC3-REPORT.md`). Пороги и API сохраняются. Эти задачи не пересекаются.

Codex, 23.09.2026 16:00: новый интерфейс реализован: навигация, схема с масштабом/полным экраном, видимое действие в карточке, выбранный перечень до 100 gid с сохранением в браузере и Markdown-экспортом. Чат сохраняет историю и корректный контекст клиента, Back возвращает карточку. Независимые проверки: перечень 22/22, навигация/клавиатура/адаптация 28/28, чат 20/20 (один настоящий live-запрос: 8,54 с, HTTP 200). Yerbu может проверять новый экран после pull.

Codex, 23.09.2026 16:08: приняты отчёт Abylaykhan7 `60602a9` и QA Yerbu `6aba32e`. Исправлены поиск запасного экрана (vis-network удалял элемент сообщения), сохранение меток выбора, контраст перечня, безопасное отображение неизвестного gid в Markdown. Новый контракт в работе по запросу Ali: `GET /api/timeline/{gid}` — фактические входящие/исходящие и рёбра по дням июля; роли и приоритет не меняются. Главный Codex владеет `app/timeline.py`, добавлением эндпоинта в main.py и `web/timeline.*`.

Codex, 23.09.2026 16:16: «Проследить переводы» реализован и проверен: 31 день, реальные суммы и пары из исходных Parquet, воспроизведение/пауза, смена даты, возврат к месяцу; обрыв не выдан за нулевой расход. Проверены все 2 248 узлов и 4 840 операций, 23 API-проверки и 25 браузерных сценариев. Следующие задания: Yerbu — `docs/PROMPT-YERBU-FINAL.md`; Abylaykhan7 — `docs/PROMPT-ABYLAYKHAN-DEMO.md`. После этого — только исправления и репетиция до фриза.

Codex: API расширен — `GET /api/routes?gid=&limit=` и поля карточки `repeated_routes`, `repeated_routes_count`, `repeated_routes_note`. Найдено 429 повторяющихся цепочек с совместимыми датами, без изменения ролей и приоритета; прежние поля сохраняются.

Codex, 23.09.2026: главный экран упрощён, `plain` есть в карточках, топе, контрагентах и узлах `/api/graph`; `/api/overview.anomalies` — число узлов с флагами. Пересчёт, 21/21 проверка CSV, чистая установка, офлайн-браузер, live/mock, отчёт и прежние API проверены. Итог и ограничения проверки — `docs/REVIEW-codex-2.md`.

Следующий шаг команды — репетиция `docs/PITCH.md` на проекторе и «Сдать решение» на платформе по инструкции организаторов, п. 10; название и описание готовы в конце PITCH. Изменение порогов не требуется.

## Договорённости (менять только всей командой)

- **Контракт фронт ↔ бэк:** `POST /api/chat` принимает `{"messages": [{"role", "content"}]}`,
  возвращает `{"reply": str, "trace": [{"tool", "args"}], "mode": "live" | "mock"}`.
  Меняешь формат — сначала пиши сюда, потом меняй код.
- **Кейс:** Freedom, «Граф денег» (AML). План и контракт API — docs/PLAN.md, ТЗ — docs/case/TZ.md.
- **Вау-фича демо:** вводим gid → граф фокусируется на узле, видно, откуда пришли и куда ушли деньги, роль объяснена цифрами; агент отвечает «кто собирает деньги с этих пятерых?»
- **Что НЕ делаем:** хардкод gid, чёрный ящик без правил, выдуманные атрибуты клиентов, авторизация и Docker.

- **Запасной экран (Ali):** `web/graph.html` → http://localhost:8000/graph.html — граф vis-network (топ-150 / окрестность узла / кластер / вся сеть), топ-лист с фильтром ролей, карточка узла, кластеры, устойчивость, чат. Открыть узел по ссылке: `/graph.html#<gid>`. Проверен в headless Chrome, ошибок в консоли нет.
- **Офлайн (Ali):** Tailwind и vis-network лежат в `web/vendor/` — подключать как `/vendor/tailwind.js` и `/vendor/vis-network.min.js`, экран работает без интернета (проверено в Chrome с заблокированной сетью).
- **Главный экран после ревью Codex:** `/` отдаёт обновлённый `web/index.html`: три KPI, топ-10, схема топ-50, прямые связи, три факта, пробелы и свёрнутые метрики/маршруты. Проверены 1600×1000 и 1280×1000 без интернета; `web/graph.html` работает как запасной.

## Сломано прямо сейчас

_Пусто. Если что-то не работает — пиши сюда сразу, а не когда заметят остальные._
