# Независимая QA — Yerbu / PC2

23.09.2026, 16:02 +05:00. База `6825fb2`, новый интерфейс проверен на `e33b9da`, свежий отчёт — на `60602a9`.
Отдельный worktree `C:/Users/2005e/hack-qa-pc2`, сервер только `127.0.0.1:8012`; исходники, STATUS, роли и пороги не менялись.

| Сценарий | Фактический результат | Баг / серьёзность / воспроизведение |
|---|---|---|
| Pipeline + валидатор | `python -m app.pipeline`: 4,6 с (процесс 5,507 с); `scripts/check_outputs.py`: 21/21 OK, свежий расчёт 2,87 с | OK; использован установленный Python из `C:/Users/2005e/hack/.venv`, зависимости не устанавливались |
| API и точность gid | 2 248 узлов / 3 119 рёбер / 72 кластера / 81 seed / 444 обрыва / 37 аномальных; все идентификаторы графа — строки из 18 цифр | OK; дублей и отсутствующих концов рёбер нет; ошибочных terminal среди 444 обрывов — 0 |
| Координатор `100000008603629100` | #5, 19 плательщиков / 1 817 300 ₸; 61 получатель / 4 986 156 ₸; на главном экране 74 узла / 104 связи со стрелками | OK; поиск Enter, карточка и API совпадают; вывод сформулирован как гипотеза |
| Аномалия `100000000332284100` | #17; 107 426 ₸ → 289 500 ₸; показано «дробление: 10 из 12 входящих… от 10 плательщ.» | OK; совпадает с API, повторено на новом интерфейсе |
| Обрыв `100000003037476100` | #583; 555 000 ₸ от 3 клиентов; «Исходящие неизвестны — обрыв на 4-м шаге», отношение «Не определено» | OK; не трактуется как удержание денег; повторено на новом интерфейсе |
| Неверный gid → правильный | `000000000000000000` даёт понятную ошибку; следующий поиск открывает карточку; после reload полный `#gid` сохранён | P3: при ошибке остаётся схема предыдущего клиента; открыть аномалию → искать нули → счётчик остаётся 14/13 |
| Кластер / вся сеть | Кластер 7: 126 узлов / 141 связь; смена цвета работает; вся сеть: 2 248 / 3 119, раскладка завершается | OK; проверено на базовом главном экране |
| Запасной `/graph.html` | Начальные топ-150/325 связей загружаются; поиск правильного gid не открывает карточку | **P1:** открыть `/graph.html` без hash → ввести `100000008603629100` → Enter; `TypeError: Cannot read properties of null (reading 'style')`, `web/graph.html:142`, `#netMsg` уже удалён vis-network; восстановление после неверного gid тоже падает |
| Чат: три подсказки | Все три вернули `mock`, содержат trace и кликабельные gid; неизвестные исходящие описаны корректно | Живой LLM не проверялся; первая подсказка с просьбой о 5 клиентах возвращает одного — ограничение mock |
| Markdown по двум gid | HTTP 200, attachment, ровно две нужные карточки; скачивание через браузер сработало | Найденная потеря «дробления» **исправлена PC3 в `60602a9`**: после перезапуска QA-сервера свежий GET содержит «дробление: 10 из 12»; отдельный отчёт обрыва корректно сообщает неизвестные исходящие |
| Новый перечень: добавить 2, убрать 1, reload | Два полных gid добавлены; координатор убран; после reload остался только `100000000332284100` | OK; счётчик и содержимое сохранены |
| Экспорт выбранных | Браузер скачал отчёт сначала для двух, затем для одного; href содержит только соответствующие выбранные gid | OK; запрос не подменяется `top=20` |
| Очистить → Вернуть | Счётчик 1→0→1; при пустом перечне экспорт недоступен; клиент восстановлен | OK; тестовый перечень после QA очищен |
| Метки выбранных в топе | После добавления первого клиента ✓ есть; после «Показать ещё» ✓ исчезает при прежнем счётчике | **P2:** добавить первую строку в перечень → «Показать ещё»; `renderTop()` пересоздаёт строки без меток; данные перечня сохранены |
| Читаемость перечня | `.review-meta` 12px, #8b9a7f на белом ≈2,99:1; `.review-notice` 12px, ≈2,91:1 | **P2:** открыть перечень; пояснения и предупреждение о хранении бледные, особенно на проекторе; `web/app.css` |
| Клавиатура | Tab из поиска → «Найти» с видимым фокусом; Enter открывает клиента; Escape закрывает перечень и возвращает фокус на `reviewOpen` | OK для перечисленных действий; полный аудит доступности не проводился |
| Ширины 1600 / 1280 / 390 | Новый экран без горизонтального переполнения; диалог на 390 px помещается (ширина 337 px) | OK; проверены фактические viewport браузера |
| Масштаб 125% | Ctrl+Plus в встроенном браузере не изменил DPR/viewport; отдельного управления zoom MCP нет | **Не проверено**; нужна ручная проверка 125% в обычном браузере |
| Локальные библиотеки | Тестовый сервер добавлял CSP с запретом внешних script/style/connect; главный экран и новый перечень работают, ошибок CSP/JS нет | OK в этой модели офлайна; физическое отключение интернета не выполнялось, порт 8000 не менялся |

Скриншоты и журнал сохранены **локально, вне Git** в `C:/Users/2005e/Documents/Codex/2026-09-23/x20/work/qa-pc2/`:
- `backup-search-error.png`, `backup-errors.json` — ошибка запасного экрана; `anomaly.png`, `truncated.png` — контрольные карточки базовой версии.
- `new-coordinator-1600.png`, `new-main-1280.png`, `new-main-390.png` — новый экран; `new-review-1280.png`, `new-review-390.png`, `review-two.png` — перечень.
- `new-selected-marker-lost.png` — потеря ✓; `local-resources-only.png` — загрузка базового экрана с внешними ресурсами, запрещёнными CSP.

Для Ali / главного Codex: сначала исправить P1 запасного экрана; затем метки выбранных и контраст. Обновление PC3 с аномалиями перепроверено. Только QA-документ отправлен в Git.
