# Финальный QA дневных переводов — Yerbu

23.09.2026; проверена база `09281bd` после `git pull --rebase`, STATUS прочитан.
Среда: Windows, Codex IAB, `127.0.0.1:8012`; desktop 1600×1000 и mobile 390×844.
Исходники и STATUS не менялись, `.env` не читался, порт 8000 не затрагивался.

## Подтверждённые дефекты

- **P1 — чужие дневные суммы при ошибке карточки.** Открыть `100000008603629100` → «Потоки по дням» → 12 июля → остановить только сервер 8012 → найти `100000000332284100`. Новая схема содержит 14 клиентов/13 связей, карточка показывает Failed to fetch, но над ней остаются 83 690 / 233 767 ₸ предыдущего клиента. Ожидается сброс дневных данных и явное состояние недоступности. После восстановления сервера и перезагрузки данные корректны.
- **P2 — стрелки клавиатуры не меняют дату.** Выбрать 12 июля, сфокусировать `#timelineRange`, нажать ←/→: остаются value=11 и 12.07.2026. Воспроизведено повторно; `interaction.keyboard:true` у vis включает обработчик window с preventDefault без исключения для range. Tab/Shift+Tab, Space на дне и воспроизведении работают.
- **P2 — ранний поиск на запасном экране.** Перезагрузить `/graph.html`, сразу ввести `100000008603629100` и нажать «Найти» до готовности графа: `TypeError: Cannot read properties of null (reading 'nodes')`, `graph.html:198`. После завершения загрузки повторный поиск открывает карточку #5 и окрестность 260/544; прежней ошибки `netMsg` нет.

## Проверено успешно

- «Проследить переводы» выбирает реального первого клиента `100000000331309100`; наблюдалась смена дат и сумм. Пауза, соседние дни, календарные столбцы и «Весь период» работают.
- Независимый проход по 4 840 исходным операциям (PyArrow + Decimal, без вызова app.timeline) совпал с API для всех 31 дня трёх контрольных клиентов: суммы, число операций и каждая направленная пара.
- Координатор `100000008603629100`: 12 июля UI/API — вход 83 690 ₸/2 операции, выход 233 767 ₸/3; на схеме 2 синие входящие и 3 зелёные исходящие стрелки. 31 июля — 0 / 340 756 ₸, 4 исходящие операции; 1 июля — пустой день.
- Контрольные пары API/raw за 12 июля: `100000003968044100 → 100000008603629100` 30 000 ₸; `100000005910114100 → 100000008603629100` 53 690 ₸; `100000008603629100 → 100000000795374100` 50 000 ₸. Передача from/to в canvas проверена по коду; каждый полный gid на canvas отдельно не считывался.
- Оговорки UI явно объясняют неизвестный порядок внутри дня и отсутствие доказательства движения тех же средств; роли остаются гипотезами.
- `100000003037476100`: 25 июля вход 465 000 ₸/2 операции; исходящие «Не наблюдаются / Обрыв на 4-м шаге». Изолированный seed `100000005235389100` открывается, все дни пустые, падения нет.
- Смена клиента, раскраска по кластеру, топ-50 (50/69), POST-пересчёт и повторное открытие дневного режима работают; пересчёт HTTP 200, показатели 2 248/17/37 сохранены.
- Mobile 390×844: горизонтального переполнения нет; даты, суммы, поиск, пауза и выбор дня доступны. На desktop различимы направления стрелок.
- В обычных сценариях главного экрана ошибок консоли нет. Ошибки сети в тесте остановленного API ожидаемы; отдельная ошибка раннего поиска описана выше.
- Главный и запасной экраны проверены с временным CSP, запрещающим внешние ресурсы/соединения: нарушений CSP нет. Статически подключения скриптов/стилей локальные. Физическое отключение интернета не выполнялось.

## Первые 10 секунд глазами жюри

Кнопка начала видна; дата подписана как наблюдаемые переводы, суммы связаны с направлением стрелок. Карточка ведёт к «Что проверить дальше» и перечню. Для уверенного демо устранить P1 и клавиатурный P2; до исправления не демонстрировать смену клиента при сбое API.

Отчёт содержит только выполненные проверки; исходники не исправлялись. Проверочный сервер остановлен после QA.
