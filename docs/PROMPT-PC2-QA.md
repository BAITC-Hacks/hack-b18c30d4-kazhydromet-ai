# Yerbu — независимая проверка продукта

Ты работаешь в репозитории Kazhydromet AI, кейс Freedom «Граф денег». Главный Codex сейчас полностью обновляет `web/index.html`, `web/app.css`, `web/review-list.js`. Не редактируй эти файлы и backend. Твоя задача — найти реальные проблемы сценария аналитика и записать проверяемый результат только в `docs/QA-PC2.md`.

1. `git pull --rebase`, прочитай AGENTS.md, docs/STATUS.md, README.md, docs/case/TZ.md. Новых зависимостей не добавляй, `.env` не читай. Порт 8000 не трогай. Проверочный сервер только `127.0.0.1:8012` на твоём компьютере.
2. Запусти pipeline и scripts/check_outputs.py. Через браузер проверь главный и запасной экраны, поиск полного gid, карточки `100000008603629100`, `100000000332284100`, `100000003037476100`, неверный gid и восстановление после ошибки, граф/стрелки/кластеры, чат и Markdown-отчёт. Реальные числа сверь с API.
3. После появления в Git нового интерфейса повтори pull и проверь выбранный перечень: добавить 2 клиентов, убрать одного, обновить страницу, скачать отчёт именно по выбранным gid, очистить и отменить очистку. Проверь клавиатуру, масштаб 125%, ширины 1600/1280/390 и офлайн-загрузку библиотек.
4. До 40 строк в отчёте: сценарий → реальный результат → баг/серьёзность → точное воспроизведение. Приложи пути скриншотов. Не выдумывай прохождение проверок, которые не запускал.
5. Коммить только `docs/QA-PC2.md`, pull --rebase и push. STATUS не меняй. Исправления передай главному Codex через Ali; не правь чужие файлы.

Критично: 18-значный gid — строка; 4-е колено означает обрыв, а не удержание денег; выводы — гипотезы. Роли и приоритеты менять нельзя.
